apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homepage
  labels:
    app: homepage
spec:
  type: NodePort
  selector:
    app: homepage
  ports:
  - name: http
    port: 3000
    targetPort: http
    nodePort: 30000
    protocol: TCP
